<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PartPoint.Views.Main.AccountPage"
             Shell.TabBarIsVisible="False"
             Title="Аккаунт">
    <ScrollView>
        <VerticalStackLayout>
            <Button BackgroundColor="{x:StaticResource Pink100}" Text="Выйти" Command="{Binding SignOutCommand}" Margin="10"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>